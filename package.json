{
  "name": "remotion-lab",
  "version": "0.1.0",
  "private": true,
  "description": "A source-first Remotion component library for animated charts, cards, titles, transitions, subtitles, and scene templates.",
  "type": "module",
  "scripts": {
    "check:syntax": "node scripts/syntax-check.mjs"
  },
  "peerDependencies": {
    "react": ">=18",
    "remotion": ">=4"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
